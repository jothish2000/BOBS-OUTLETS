/* BOBS Method 2 item UX v2 — additive UI: auto capacity, multi-select sides, shared serving/packing rules. Method 1 untouched. */
(function(){'use strict';
const $=id=>document.getElementById(id),num=v=>Number.isFinite(Number(v))?Math.max(0,Number(v)):0;
function fire(el){el.dispatchEvent(new Event('input',{bubbles:true}));el.dispatchEvent(new Event('change',{bubbles:true}))}
function labelInput(box,text){return [...box.querySelectorAll('label')].find(l=>l.childNodes[0]&&String(l.childNodes[0].textContent).trim().startsWith(text))?.querySelector('input')}
function installCapacity(){const cap=$('capacity'),batch=$('batchSize'),today=$('batches'),lab=$('capacityLabel');if(!cap||!batch||!today||!lab||$('maxBatchesDay'))return;
 const wrap=document.createElement('label');wrap.id='maxBatchesLabel';wrap.textContent='Maximum batches / day';const max=document.createElement('input');max.id='maxBatchesDay';max.type='number';max.min='0';max.step='1';wrap.append(max);lab.parentNode.insertBefore(wrap,lab);
 function initialise(){const b=num(batch.value),c=num(cap.value),t=num(today.value);max.value=b>0&&c>0?Math.max(t,Math.round(c/b)):t||'';update(false)}
 function update(mark=true){const b=num(batch.value),m=num(max.value);cap.value=b&&m?String(b*m):'';cap.readOnly=true;cap.title='Auto-calculated: quantity per batch × maximum batches/day';if(mark)fire(cap)}
 batch.addEventListener('input',()=>update());max.addEventListener('input',()=>update());setTimeout(initialise,80);
}
function installMultiSides(){const select=$('condimentChoice'),add=$('addCondiment');if(!select||!add||$('m2MultiSides'))return;
 const box=document.createElement('div');box.id='m2MultiSides';box.className='component';box.innerHTML='<h3>Select all condiments / poriyal served</h3><p class="muted">Tick any number of sides. Each selected side keeps its own Recipe Master link and COGS.</p><div id="m2SideChecks" style="display:grid;grid-template-columns:repeat(auto-fit,minmax(180px,1fr));gap:6px"></div>';
 add.parentNode.parentNode.insertBefore(box,add.parentNode);const checks=box.querySelector('#m2SideChecks');
 [...select.options].forEach(o=>{if(!o.value)return;const l=document.createElement('label');l.style.margin='0';const cb=document.createElement('input');cb.type='checkbox';cb.value=o.value;l.append(cb,document.createTextNode(' '+o.textContent));checks.append(l);cb.onchange=()=>{const existing=[...$('condiments').querySelectorAll('h3 a')].some(a=>a.textContent.toLowerCase().startsWith(o.value.toLowerCase()));if(cb.checked&&!existing){select.value=o.value;add.click()}else if(!cb.checked&&existing){const comp=[...$('condiments').querySelectorAll('.component')].find(c=>c.querySelector('h3 a')?.textContent.toLowerCase().startsWith(o.value.toLowerCase()));comp?.querySelector('button.secondary')?.click()}setTimeout(sync,30)}});
 add.parentNode.style.display='none';
 function sync(){const names=[...$('condiments').querySelectorAll('h3 a')].map(a=>a.textContent.split(' · ')[0].toLowerCase());checks.querySelectorAll('input').forEach(c=>c.checked=names.includes(c.value.toLowerCase()));installServingRules()}
 new MutationObserver(sync).observe($('condiments'),{childList:true});setTimeout(sync,100);
}
function installServingRules(){[...$('condiments').querySelectorAll('.component')].forEach(comp=>{if(comp.querySelector('.m2ServingRule'))return;const portion=labelInput(comp,'Portion per sales unit');if(!portion)return;const unitSel=[...comp.querySelectorAll('label')].find(l=>String(l.childNodes[0]?.textContent||'').trim().startsWith('Portion unit'))?.querySelector('select');const rule=document.createElement('div');rule.className='m2ServingRule notice';rule.innerHTML='<b>Shared serving / pack rule</b><div class="fields"><label>Order size (sales units)<input class="m2OrderUnits" type="number" min="1" step="1" value="1"></label><label>Physical side pack quantity<input class="m2PackQty" type="number" min="0" step="any"></label></div><small>Example logic: if one 100 ml sambar pouch serves 2 idlis, enter order size 2 and pack quantity 100 ml. BOBS allocates 50 ml COGS per idli; staff still issues one standard pouch.</small>';
 comp.insertBefore(rule,comp.querySelector('button.secondary'));const ou=rule.querySelector('.m2OrderUnits'),pq=rule.querySelector('.m2PackQty');pq.value=num(portion.value)||'';
 function apply(){const n=num(ou.value)||1,q=num(pq.value);portion.value=q/n;fire(portion)}ou.oninput=apply;pq.oninput=apply;if(unitSel)unitSel.addEventListener('change',()=>{});
 })}
const PACKS=['Idli container / tray','Food container - small','Food container - medium','Food container - large','Sambar pouch / cup - small','Sambar pouch / cup - medium','Chutney pouch / cup','Container lid','Butter paper / food sheet','Carry bag','Spoon / fork','Rubber band / sealing item','Other packing'];
function installPackingPicker(){const add=$('addPacking'),packing=$('packing');if(!add||!packing||$('m2PackingPicker'))return;const box=document.createElement('div');box.id='m2PackingPicker';box.className='component';box.innerHTML='<h3>Packing catalogue</h3><p class="muted">Select a component, then enter your actual unit cost. Multiple packing components can be combined.</p><div class="toolbar"><select id="m2PackChoice"></select><button type="button" id="m2PackAdd">+ Add selected packing</button></div>';packing.parentNode.insertBefore(box,packing);const sel=box.querySelector('select');PACKS.forEach(n=>{const o=document.createElement('option');o.value=n;o.textContent=n;sel.append(o)});box.querySelector('button').onclick=()=>{add.click();setTimeout(()=>{const comps=[...packing.querySelectorAll('.component')],c=comps[comps.length-1];const name=labelInput(c,'Component');if(name){name.value=sel.value;fire(name)}},10)};add.textContent='+ Custom packing / other cost';
 const pp=$('packingPer');if(pp){const l=pp.closest('label');if(l)l.childNodes[0].textContent='Order size / sales units sharing this parcel';}
 const note=document.createElement('p');note.className='notice';note.textContent='Shared packing rule: total selected packing cost for one parcel ÷ order size. Example: one 4-idli container + lid + bag is divided by 4 for COGS per idli.';packing.parentNode.append(note);
}
function boot(){installCapacity();installMultiSides();installPackingPicker();setInterval(installServingRules,700)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(boot,150));else setTimeout(boot,150);
})();