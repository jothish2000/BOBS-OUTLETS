/* BOBS Global Saved-Data Decision Layer — Google-first + first-time recovery dataset */
(function(){
const path=(location.pathname.split('/').pop()||'').toLowerCase();
const configs={'method1.html':{title:'Existing Method 1 data found',key:'method1-hourly-state',research:true,label:'Method 1'},'method2.html':{title:'Existing Method 2 data found',key:'method2-item-state',research:true,label:'Method 2'},'staff.html':{title:'Existing Staff / HR data found',key:'staff-data',research:false,label:'Staff / HR'},'fixed-expenses.html':{title:'Existing Fixed Expense data found',key:'fixed-expenses-data',research:false,label:'Fixed Expenses'},'production.html':{title:'Existing Production data found',key:'production-data',research:true,label:'Production'},'roster.html':{title:'Existing Roster data found',key:'roster-data',research:false,label:'Roster'}};
const cfg=configs[path];if(!cfg)return;
const FIRST_TIME_M1={outletId:'1',outletName:'Rasipuram',date:'2026-08-08',custVals:[12,28,42,38,22,14,11,16,13,9,12,20,38,48,40,27,16],basketVals:[35,38,42,40,38,35,35,45,42,35,38,45,52,58,55,48,40],source:'RECOVERED_DATASET'};
function validM1(v){return !!(v&&Array.isArray(v.custVals)&&Array.isArray(v.basketVals)&&v.custVals.length===17&&v.basketVals.length===17)}
function hasSaved(){try{const v=JSON.parse(localStorage.getItem(cfg.key)||'null');if(cfg.key==='method1-hourly-state')return validM1(v);return !!v&&v!=='{}'&&v!=='[]'&&v!=='null'}catch(e){return false}}
function currentSaved(){try{return JSON.parse(localStorage.getItem(cfg.key)||'null')}catch(e){return null}}
function datasets(){try{return JSON.parse(localStorage.getItem('bobs-research-datasets')||'[]')||[]}catch(e){return []}}
function loadProtection(){return new Promise((resolve,reject)=>{if(window.BOBSProtection){resolve();return}const s=document.createElement('script');s.src='bobs-protection.js?v=20260908-recovery5';s.onload=()=>window.BOBSProtection?resolve():reject(new Error('Protection controller unavailable'));s.onerror=()=>reject(new Error('Protection controller could not be loaded'));document.head.appendChild(s)})}
function outletId(){try{const o=JSON.parse(localStorage.getItem('outlet-selection')||'null')||{};return String(o.id||new URLSearchParams(location.search).get('outlet')||'')}catch(e){return String(new URLSearchParams(location.search).get('outlet')||'')}}
function applyMethod1State(st){try{localStorage.setItem('method1-hourly-state',JSON.stringify(st))}catch(e){};(st.custVals||[]).forEach((v,i)=>{const el=document.querySelector('.custInput[data-i="'+i+'"]');if(el)el.value=v});(st.basketVals||[]).forEach((v,i)=>{const el=document.querySelector('.basketInput[data-i="'+i+'"]');if(el)el.value=v});if(document.getElementById('outletId'))document.getElementById('outletId').value=st.outletId||'';if(document.getElementById('outletName'))document.getElementById('outletName').value=st.outletName||'';if(typeof window.recalc==='function')window.recalc()}
async function seedFirstTimeMethod1(googleResult){
 if(cfg.key!=='method1-hourly-state'||outletId()!=='1')return null;
 /* Google was tried first. If it did not return an existing valid METHOD1 record, use the explicitly supplied first-time dataset. */
 if(!googleResult||googleResult.found===true)return null;
 const existing=currentSaved();
 if(validM1(existing)&&existing.source==='RECOVERED_DATASET'){applyMethod1State(existing);return existing;}
 /* Preserve unrelated working edits; only seed when Method 1 has no valid working state. */
 if(validM1(existing))return null;
 const state={...FIRST_TIME_M1,savedAt:new Date().toISOString()};applyMethod1State(state);return state;
}
function modal(recoveredState){
const s=document.createElement('style');s.textContent='.bobs-decision-overlay{position:fixed;inset:0;background:rgba(0,0,0,.52);display:flex;align-items:center;justify-content:center;padding:18px;z-index:100000}.bobs-decision{width:min(560px,100%);background:var(--paper,#fff);color:var(--ink,#222);border-radius:14px;padding:22px;box-shadow:0 20px 70px rgba(0,0,0,.3)}.bobs-decision h2{margin:0 0 8px;font-size:20px}.bobs-decision p{font-size:13px;line-height:1.5;color:var(--ink-soft,#666)}.bobs-decision-actions{display:grid;gap:9px;margin-top:16px}.bobs-decision-actions button{min-height:48px;border:1px solid rgba(0,0,0,.15);border-radius:9px;padding:9px 13px;text-align:left;cursor:pointer;font-weight:600}.bobs-keep{background:var(--profit,#2f7d32);color:#fff}.bobs-add{background:var(--paper,#fff)}.bobs-new{background:transparent}.bobs-save-recovered{background:#fff7d6;border-color:#d6ad32!important}.bobs-research{background:var(--paper-dark,#f5f5f5)}.bobs-dataset-list{margin-top:10px;max-height:160px;overflow:auto}.bobs-dataset-list button{width:100%;margin:4px 0}';document.head.appendChild(s);
const ov=document.createElement('div');ov.className='bobs-decision-overlay';const box=document.createElement('div');box.className='bobs-decision';
const recoveredText=recoveredState?'<p><b>No usable existing Method 1 record was returned for Outlet 1.</b> BOBS has loaded the original 17-hour Method 1 dataset you supplied for the first-time recovery. It is editable and is not yet permanently saved in Google.</p>':'<p>BOBS found saved information for <b>'+cfg.label+'</b>. The permanent record is kept in the Google Data Vault.</p>';
box.innerHTML='<h2>'+(recoveredState?'First-time Method 1 recovery data loaded':cfg.title)+'</h2>'+recoveredText+'<p><b>What would you like to do?</b></p><div class="bobs-decision-actions"><button class="bobs-keep">A. Continue Using Existing Data</button><button class="bobs-add">B. Add / Modify Existing Data</button><button class="bobs-new">C. Start Fresh</button>'+(recoveredState?'<button class="bobs-save-recovered">★ Save this recovered data permanently to Google</button>':'')+(cfg.research?'<button class="bobs-research">D. Use a saved research dataset</button>':'')+'<div class="bobs-dataset-list" style="display:none"></div></div>';ov.appendChild(box);document.body.appendChild(ov);
box.querySelector('.bobs-keep').onclick=()=>ov.remove();box.querySelector('.bobs-add').onclick=()=>ov.remove();
const saveRecovered=box.querySelector('.bobs-save-recovered');if(saveRecovered)saveRecovered.onclick=async()=>{saveRecovered.disabled=true;saveRecovered.textContent='Saving permanently to Google…';try{const st=JSON.parse(localStorage.getItem('method1-hourly-state')||'null');const ok=window.BOBS_METHOD1_GOOGLE_SAVE?await window.BOBS_METHOD1_GOOGLE_SAVE(st):false;if(!ok)throw new Error('Google save service is not available');st.source='GOOGLE_SHEETS';st.savedAt=new Date().toISOString();localStorage.setItem('method1-hourly-state',JSON.stringify(st));saveRecovered.textContent='✓ Saved permanently to Google';setTimeout(()=>ov.remove(),900)}catch(e){saveRecovered.disabled=false;saveRecovered.textContent='★ Save this recovered data permanently to Google';alert('The recovered dataset was not cleared or lost. Google permanent save could not be confirmed.\n\n'+e.message)}};
box.querySelector('.bobs-new').onclick=async()=>{if(!confirm('Start Fresh will first protect this module in the BOBS Data Vault, then clear only the current working data. The permanent Google record will not be deleted. Continue?'))return;const btn=box.querySelector('.bobs-new');btn.disabled=true;btn.textContent='Protecting snapshot…';try{await loadProtection();await window.BOBSProtection.protectAndPrepareFresh('Start Fresh — '+cfg.label,[cfg.key]);btn.textContent='Protected ✓';location.reload()}catch(e){btn.disabled=false;btn.textContent='C. Start Fresh';alert('BOBS did not clear the data because the protection step was not completed. Your saved working data remains intact.\n\n'+e.message)}};
const rb=box.querySelector('.bobs-research');if(rb)rb.onclick=()=>{const list=box.querySelector('.bobs-dataset-list'),ds=datasets();list.style.display='block';if(!ds.length){list.innerHTML='<p>No saved research datasets are available yet.</p>';return}list.innerHTML=ds.map((d,i)=>'<button data-i="'+i+'">'+(d.name||('Research dataset '+(i+1)))+(d.location?' — '+d.location:'')+'</button>').join('');list.querySelectorAll('button').forEach(b=>b.onclick=()=>{localStorage.setItem('bobs-active-research-dataset',JSON.stringify(ds[+b.dataset.i]));ov.remove();location.reload()})};
}
async function gate(){let googleResult=null;try{if(window.BOBS_RECOVERY_PROMISE)await window.BOBS_RECOVERY_PROMISE}catch(e){}try{if(window.BOBS_GOOGLE_LOAD_PROMISE)googleResult=await window.BOBS_GOOGLE_LOAD_PROMISE}catch(e){googleResult={found:false,error:e.message||String(e)}}let recovered=null;if(cfg.key==='method1-hourly-state')recovered=await seedFirstTimeMethod1(googleResult);if(hasSaved())modal(recovered)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',gate,{once:true});else gate();
})();